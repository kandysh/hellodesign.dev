{
  "name": "hellodesign-dev",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "format": "biome format --write .",
    "check": "biome check .",
    "db:migrate": "pnpm turbo run db:migrate",
    "db:deploy": "pnpm turbo run db:deploy",
    "db:generate": "pnpm turbo run db:generate"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.4",
    "turbo": "^2.9.6",
    "typescript": "^5.9.3"
  },
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20"
  }
}
